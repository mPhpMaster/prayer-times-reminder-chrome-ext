{
  "name": "prayer-game-server",
  "version": "0.1.0",
  "private": true,
  "description": "Game API (accounts, progress sync, follows, monthly leaderboard). Database-agnostic: DB_KIND=memory|postgres|mysql.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/main.mjs",
    "migrate": "node src/migrate.mjs",
    "test": "node --test test/api.test.mjs"
  },
  "optionalDependencies": {
    "mysql2": "^3.11.0",
    "pg": "^8.13.0"
  }
}
